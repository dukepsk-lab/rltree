# Walk-Forward Plan — XAUUSD_DayOpen_Long_Robust (demo account)

เป้าหมาย: พิสูจน์ว่า plateau ที่พบจากการ optimize (TP 1.0–1.25% × SL 8–10%) ใช้ได้กับข้อมูล
ที่โมเดล**ไม่เคยเห็น** ก่อนตัดสินใจใด ๆ เกี่ยวกับเงินจริง หลักฐานปัจจุบัน (backtest H1 2026,
62 ไม้, Z-score 1.47) ยังบางเกินไป — demo forward คือขั้นตอนที่ถูกต้อง

---

## 1. สองขั้นที่ควรทำคู่กัน

### ขั้น A — Tester walk-forward (ทำได้วันนี้ ไม่ต้องรอ)
รัน optimization แบบแบ่ง in-sample / out-of-sample ด้วย `optimize_tp_walkforward.ini`:
MT5 จะ optimize บนช่วง ม.ค.–เม.ย. แล้วทดสอบค่าที่ได้กับ พ.ค.–มิ.ย. ที่ optimizer ไม่เคยเห็น
(`ForwardMode=2` = สงวน 1/3 ท้ายเป็น forward period)

**เกณฑ์อ่านผล:** ดูแท็บ "Forward Results" — เซลล์ใน plateau (TP 1.0–1.25 × SL 8–10)
ต้องยังเป็นบวกในช่วง forward ถ้า plateau หายในช่วง forward = สัญญาณ overfit ของทั้งระบบ
ให้หยุดก่อน demo

### ขั้น B — Demo forward (ของจริง เก็บขั้นต่ำ 8–12 สัปดาห์)

## 2. คอนฟิกที่ deploy บน demo

| รายการ | ค่า |
|---|---|
| EA | `XAUUSD_DayOpen_Long_Robust.mq5` **v2.12** (คอมไพล์ใหม่ใน MetaEditor) |
| Chart | XAUUSD, D1, demo account (IUXMarkets demo ที่ใช้ backtest จะเทียบง่ายสุด) |
| Inputs หลัก | ค่า default ทั้งหมด: TP 1.0% / SL 9% / breaker 30% / ADX filter / magic 20260703 |
| ทุนเริ่ม | $1,000 (ตรงกับรายงาน `reports/ReportTester2101171557_Top_Deposit1000.xlsx` ที่ใช้เป็น baseline) |

**ทางเลือก (แนะนำ):** รันคู่ขนานตัวที่สองบน chart แยกเพื่อเทียบ candidate สองตัวพร้อมกัน —
ตั้ง `InpDailyProfitPct=1.25`, `InpMaxDailyLossPct=10`, และ**ต้องเปลี่ยน** `InpMagic`
(เช่น 20260704) กับ `InpRunTag` ไม่ให้ชนกัน

ข้อกำหนดระบบ: เทอร์มินัลต้องออนไลน์ต่อเนื่อง (แนะนำ VPS) — broker-side SL/TP คุ้มครองตอน
หลุดการเชื่อมต่อ แต่การปิดสิ้นวัน (EOD close) ต้องให้ EA ทำงานตอนเที่ยงคืนเซิร์ฟเวอร์;
ถ้าเทอร์มินัลดับข้ามวัน position จะถูกปิดตอนกลับมาออนไลน์แทน

## 3. Expectation bands — ใช้จับว่า "ยังตรงสเปก" หรือ "พัง"

จาก backtest baseline (TP 1.0–1.25 / SL 9–10, ทุน $1,000):

| ตัวชี้วัด | คาดหวัง | ระดับผิดปกติ (ให้เริ่มสงสัย) |
|---|---|---|
| Win rate สะสม | ~88–97% | < 80% เมื่อมี ≥ 20 ไม้ |
| กำไรไม้ชนะ | ≈ +1.0–1.25% ของ equity | ต่ำกว่าเป้าบ่อย ๆ = spread/slippage demo แย่กว่า backtest |
| ขาดทุนไม้แพ้ | ≈ −9 ถึง −10% ของ equity | เกิน −11% = SL หลุด/slippage ผิดปกติ |
| เวลาถือเฉลี่ย | หลักนาที (backtest: 4m18s) | ถือเป็นชั่วโมงบ่อย ๆ = ตลาดเปลี่ยนสภาพ |
| จำนวนเทรด | ~2–3 ไม้/สัปดาห์ (ADX คัดออกราวครึ่ง) | 0 ไม้หลายสัปดาห์ = filter ตันผิดปกติ |
| Equity DD | ≤ ~14% | > 15% = เกินทุกอย่างที่เคยเห็นใน backtest |

## 4. กติกาหยุด (เขียนไว้ก่อน ไม่ต่อรองทีหลัง)

1. **หยุดทันที** ถ้า circuit breaker ตัด (DD 30% จากจุดสูงสุด) — ระบบออกแบบให้จุดนี้คือ "จบ"
2. **หยุดและทบทวน** ถ้า DD > 15% หรือโดน SL เต็ม ≥ 2 วันภายใน 5 วันเทรด
3. **หยุดและทบทวน** ถ้าไม้แพ้ลึกกว่า −11% ของ equity (SL ไม่ทำงานตามสเปก)
4. ไม่แก้พารามิเตอร์ระหว่างทาง — ถ้าอยากแก้ แปลว่าเริ่มนับ walk-forward ใหม่

## 5. รีวิวรายสัปดาห์

ทุกสัปดาห์ export history จาก MT5 (แท็บ History → คลิกขวา → Report) เก็บลง `reports/`
แล้วบันทึกตาราง:

| สัปดาห์ | ไม้ | ชนะ | แพ้ | P/L $ | Equity | DD สะสม | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| 1 | | | | | | | |

## 6. เกณฑ์ "ผ่าน" เมื่อครบ 8–12 สัปดาห์ (หรือ ≥ 40 ไม้)

- กำไรสุทธิเป็นบวก และ DD ไม่เกิน 15%
- Win rate / ขนาดไม้แพ้ อยู่ใน band ตามข้อ 3
- ไม่ชนกติกาหยุดข้อใดเลย

ผ่านทั้งหมด → ค่อยพิจารณาเงินจริงด้วย**ทุนที่เสียได้จริงเท่านั้น** และเผื่อใจว่า demo ให้
spread/slippage ดีกว่าบัญชีจริงเสมอ — ผล demo คือ "เพดาน" ไม่ใช่ "ค่ากลาง"
ไม่ผ่าน → เอา report กลับมาวิเคราะห์ด้วย `analyze_optimizer_report.py` / อัปเดต plateau ใหม่

---
*อัปโหลดไฟล์ report ที่ export มาได้เลย — มีเครื่องมือวิเคราะห์รออยู่ในรีโปนี้แล้ว*
